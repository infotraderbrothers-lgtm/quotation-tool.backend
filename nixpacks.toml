[phases.setup]
nixPkgs = ['nodejs-18_x']

[phases.install]
cmds = ['npm install']

[start]
cmd = 'node server.js'
```

**What changed:**
- Removed Chromium system packages (not needed anymore)
- Much faster build time
- The `@sparticuz/chromium` package handles everything

---

## 📄 FILE 4: Procfile (Keep this if you have it)
```
web: node server.js
